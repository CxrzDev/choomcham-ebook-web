# 🚀 สิ่งที่ต้องทำเพื่อให้เว็บไซต์พร้อมใช้งานจริง (Ready to Launch Checklist)

เว็บไซต์ตอนนี้ Frontend (หน้าบ้าน) เสร็จสมบูรณ์สวยงามแล้วครับ แต่เพื่อให้ระบบทำงานได้ครบวงจร (เก็บเงินได้, เก็บข้อมูลลูกค้าได้, ส่งของได้) ต้องตั้งค่า Backend (หลังบ้าน) เพิ่มเติมดังนี้ครับ:

## 1. 💳 ระบบรับชำระเงิน (Payment Gateway)
> **สถานะปัจจุบัน:** เป็น Demo Mode (กดจ่ายเงินแล้วขึ้น Alert แจ้งเตือนเฉยๆ)

**สิ่งที่ต้องทำ:**
1.  สมัครบัญชี **Omise** (หรือ Stripe / GB Prime Pay ตามสะดวก)
2.  นำ **Public Key** และ **Secret Key** มาใส่ในไฟล์ `.env` และใน Vercel Settings
3.  เปิดไฟล์ `src/app/checkout/page.tsx` (หรือหน้าที่เกี่ยวข้อง) เพื่อปลดล็อกโค้ดส่วนที่เชื่อมต่อ API จริง
    *   *ตอนนี้โค้ดส่วนนั้นอาจจะถูก Comment ไว้ หรือต้องเขียนเพิ่มในส่วน `/api/checkout`*

---

## 2. 🗄️ ฐานข้อมูลลูกค้า (Supabase Database)
> **สถานะปัจจุบัน:** มี Code เชื่อมต่อแล้ว แต่ถ้า Database ยังไม่มีตาราง ข้อมูลจะบันทึกไม่ได้

**สิ่งที่ต้องทำ:**
1.  เข้าไปที่ **Supabase Dashboard** ของโปรเจกต์นี้
2.  ไปที่ **SQL Editor** แล้วรันคำสั่งนี้เพื่อสร้างตาราง:
    ```sql
    create table registrations (
      id bigint generated by default as identity primary key,
      created_at timestamp with time zone default timezone('utc'::text, now()) not null,
      nickname text,
      email text,
      role text,
      pain_point text,
      consent boolean
    );
    ```
3.  **ตั้งค่า RLS (Row Level Security):** เปิดให้ "Insert" ข้อมูลได้ (เพื่อให้คนทั่วไปลงทะเบียนได้)

---

## 3. 📧 ระบบส่งอีเมลอัตโนมัติ (Email Automation)
> **สถานะปัจจุบัน:** ยังไม่มีระบบส่งไฟล์ E-Book ให้ลูกค้าอัตโนมัติ

**สิ่งที่ต้องทำ:**
1.  เลือกใช้บริการส่งอีเมล (แนะนำ **Resend.com** เพราะใช้ง่ายและฟรีสำหรับเริ่มต้น)
2.  สมัครและเอา **API Key** มาใส่
3.  สร้าง API Route (`src/app/api/send-email/route.ts`) เพื่อรับคำสั่งว่า "ถ้าลูกค้าจ่ายเงินสำเร็จ -> ให้ส่งลิงก์ PDF ไปที่อีเมลลูกค้าทันที"

---

## 4. 🖼️ โลโก้และรูปภาพ (Assets)
> **สถานะปัจจุบัน:** ใช้รูป `cropped-logo.png` (ต้องเช็คว่าถูกต้องไหม)

**สิ่งที่ต้องทำ:**
*   ตรวจสอบว่าโลโก้ในหน้า Quiz และหน้าอื่นๆ ถูกต้องตาม Branding
*   ถ้ายังไม่ใช่ ให้อัปโหลดไฟล์ใหม่ไปที่ `public/images/` ชื่อไฟล์เดิม หรือแก้ Code ให้ชี้ไปที่ไฟล์ใหม่

---

## 5. ⚙️ การตั้งค่าบน Vercel (Production Environment)
> **สำคัญมาก!** ถ้าไม่ใส่ Key ใน Vercel เว็บจริงจะใช้งานไม่ได้

**สิ่งที่ต้องทำ:**
1.  เข้า Vercel > Project Settings > **Environment Variables**
2.  ใส่ค่าเหล่านี้ให้ครบ:
    *   `NEXT_PUBLIC_SUPABASE_URL`
    *   `NEXT_PUBLIC_SUPABASE_ANON_KEY`
    *   `NEXT_PUBLIC_OMISE_PUBLIC_KEY` (ถ้ามี)
    *   `OMISE_SECRET_KEY` (ถ้ามี)
    *   `RESEND_API_KEY` (ถ้ามี)

---

**✅ หากทำครบทุกข้อ เว็บไซต์จะสามารถ:**
1.  เก็บเงินลูกค้าได้จริง
2.  ลูกค้าได้ไฟล์หนังสือทันทีทางอีเมล
3.  เจ้าของแบรนด์ได้รายชื่อลูกค้า (Lead) ไปต่อยอดขายคอร์สได้ทันที!
